# Benchmark Package

Reusable benchmark helpers for comparing memory candidates on the same gold set.

This package intentionally ships only:

- generic candidate metadata
- gold-set builders
- aggregation helpers
- test coverage
- adapter scaffolds

It does not ship private corpora or real benchmark results.
