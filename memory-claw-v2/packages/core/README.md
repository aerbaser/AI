# Core Policy And Schemas

This package holds machine-readable files that describe the recommended memory stack.

- `policies/`: default memory policy for OpenClaw-style agents
- `schemas/`: normalized event and record schemas for automatic ingestion
