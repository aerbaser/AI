# Local Codex Proxy

OpenAI-compatible `chat/completions` proxy backed by `codex exec`.

Default runtime:

- model: `gpt-5.4-mini`
- host: `127.0.0.1`
- port: `4321`

This is intended for local extraction and memory-write paths, not for exposing a public API.
